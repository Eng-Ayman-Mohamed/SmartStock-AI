# SmartStock AI — Frontend environment configuration
# Copy this file to `.env.local` and adjust as needed.

# Backend API URL — proxied through Vite in dev, absolute in production
# Dev:    /api  (proxied through vite.config.ts)
# Prod:   https://<your-render-backend>/api
VITE_API_URL=/api

# When true, authentication is bypassed for development
VITE_AUTH_BYPASS=false

# Production backend URL (set in Vercel dashboard for production deployments)
# VITE_API_BASE_URL=https://smartstock-api.onrender.com
